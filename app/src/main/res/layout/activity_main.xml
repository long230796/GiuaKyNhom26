<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.nhom26.giuakynhom26.activities.MainActivity"
    android:weightSum="1">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="78dp">

        <TextView
            android:text="Chương trình quản lý thiết bị phòng học"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/txtProgramName"
            android:textStyle="normal|bold"
            android:textAlignment="center"
            android:textSize="33sp" />
    </LinearLayout>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="184dp">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="184dp"
            app:srcCompat="@mipmap/ic_launcher"
            android:id="@+id/imgMain" />
    </LinearLayout>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="188dp"
        android:layout_weight="0.83">

        <Button
            android:text="Quản lý loại"
            android:id="@+id/btnLoai"
            style="@style/button_long" />

        <Button
            android:text="Quản lý thiết bị"
            android:id="@+id/btnThietbi"
            android:background="@drawable/custom_button"
            style="@style/button_long" />

        <Button
            android:onClick="quanLyPhong"
            android:text="Quản lý phòng"
            android:id="@+id/btnPhong"
            android:background="@drawable/custom_button"
            style="@style/button_long" />

        <Button
            android:onClick="thongTinPhanMem"
            android:text="Chi tiết phần mềm"
            android:id="@+id/btnChitiet"
            android:background="@drawable/custom_button"
            style="@style/button_long"/>
    </LinearLayout>
</LinearLayout>
