<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nhom26.giuakynhom26">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label=" ">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.PhongActivity"
            android:label="@string/phong_name" />
        <activity android:name=".activities.LoaiTBActivity" />
        <activity android:name=".activities.ThietBiActivity" />
        <activity
            android:name=".activities.PhongActivity"
            android:label="@string/phong_name" />
        <activity
            android:name=".activities.ChitietsudungPhongActivity"
            android:label="Chi tiết sử dụng"></activity>
        <activity android:name=".activities.EditThietBiActivity"></activity>
    </application>

</manifest>